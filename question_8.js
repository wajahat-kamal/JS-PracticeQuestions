

function regexVar(s) {

    let vowel = "a" || "e" || "i" || "o" || "u"

    
    if (s.startsWith(vowel) && s.endsWith()) {
        
    }
    
    
    /*
     * Do not remove the return statement
     */
    return re;
}
console.log(regexVar("abcda"))